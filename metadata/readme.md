. : root directory
├── artifacts : Dedicated for trained model, weights, evaluation metrics
├── configs : Dedicated for Global variables, hyper parameters filepaths
├── data : Dedicated for storing data
│   ├── processed_data
│   └── raw_data
├── metadata : Dedicated for readmes, ignoring files (.gitignore, .dockerignore, .claudeignore etc.)
│   └── readme.md
└── src : Main 'Package' directory 